# CLAUDE.md

Use `AGENTS.md` as the repository-wide source of truth. Do not duplicate or override it here.

Before visual work, load the relevant project skill:

- `.agents/skills/design-system/SKILL.md` for general portfolio UI.
- `.agents/skills/technical-isometric/SKILL.md` for the NRN isometric mark, modular geometry, technical ruler lines, hatch, stroke visibility, spotlight, or press interaction.

Keep changes native to Astro + TypeScript + CSS and run `pnpm build` before treating implementation work as complete.
