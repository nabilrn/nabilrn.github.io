<div class="blog-shell">
	<header class="blog-header">
		<div class="blog-header-inner">
			<a href="/blog/" class="blog-brand">nabilrn / blog</a>
			<nav class="blog-nav" aria-label="Blog navigation">
				<a href="/blog/" class="blog-nav-link">Articles</a>
				<a href="/" class="blog-nav-link">Portfolio</a>
			</nav>
		</div>
	</header>

	<main class="blog-main">
		<slot />
	</main>
</div>

<style>
	.blog-shell {
		min-height: 100vh;
	}

	.blog-header {
		position: sticky;
		top: 0;
		z-index: 20;
		background: color-mix(in srgb, var(--gh-base) 88%, transparent);
		backdrop-filter: blur(8px);
		border-bottom: 1px solid var(--gh-border);
	}

	.blog-header-inner {
		max-width: 1200px;
		margin: 0 auto;
		padding: 14px 24px;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 16px;
	}

	.blog-brand {
		font-size: 13px;
		font-weight: 600;
		color: var(--gh-text);
	}

	.blog-nav {
		display: flex;
		align-items: center;
		gap: 8px;
	}

	.blog-nav-link {
		font-size: 12px;
		color: var(--gh-text-secondary);
		padding: 4px 10px;
		border: 1px solid transparent;
		border-radius: 999px;
		transition: background-color 0.15s ease, border-color 0.15s ease, color 0.15s ease;
	}

	.blog-nav-link:hover {
		border-color: var(--gh-border-muted);
		background: var(--gh-surface);
		color: var(--gh-text);
	}

	.blog-main {
		max-width: 1200px;
		margin: 0 auto;
		padding: 32px 24px 96px;
	}

	@media (max-width: 768px) {
		.blog-header-inner {
			padding: 12px 16px;
		}

		.blog-main {
			padding: 18px 16px calc(100px + env(safe-area-inset-bottom));
		}
	}
</style>
