---
const VIEWBOX_X = 60;
const VIEWBOX_Y = 45;
const VIEWBOX_WIDTH = 825;
const VIEWBOX_HEIGHT = 470;
const PRESS_DISTANCE = 14;

// Approved top-surface geometry from the final Figma NRN mark.
const N_FRONT_PATH = 'M 0 84 L 28 70 L 162.39999389648438 137.1999969482422 L 140 14 L 168 0 L 336 84 L 308 98 L 173.60000610351562 30.799999237060547 L 196 154 L 168 168 L 0 84 Z';
const R_FRONT_PATH = 'M 168 0 L 240.36923217773438 36.18461608886719 C 277.8461570739746 54.923078536987305 283.01537322998047 74.30768966674805 257.1692199707031 87.23076629638672 C 231.32306671142578 100.15384292602539 193.84615898132324 98.21538543701172 165.41539001464844 84 L 138.27691650390625 153.13845825195312 L 102.0923080444336 135.04615783691406 L 124.06153869628906 73.66153717041016 L 99.5076904296875 61.38461685180664 L 27.13846206665039 97.56922912597656 L 0 84 L 168 0 Z M 169.2923126220703 26.492307662963867 L 214.5230712890625 49.10769271850586 C 236.49230194091797 60.09230899810791 241.66153526306152 69.13846349716187 231.3230743408203 74.30769348144531 C 220.98461246490479 79.47692441940308 202.8923110961914 76.89230823516846 180.92308044433594 65.9076919555664 L 135.6923065185547 43.29230880737305 L 169.2923126220703 26.492307662963867 Z';

const LEFT_N_MATRIX = 'matrix(-0.999965966 -0.008251472 -0.008251472 0.999965966 440.330566 75.151489)';
const RIGHT_N_MATRIX = 'matrix(-0.999965966 -0.008251472 -0.008251472 0.999965966 866.330566 275.151489)';
const R_MATRIX = 'matrix(0.999965966 0.008251472 -0.008251472 0.999965966 332.126709 179.541138)';

// Exact swept-solid boundaries exported from the approved Figma studies.
// NORMAL = sweep 0..28px. PRESSED = sweep 14..28px with the bottom plane fixed.
const N_SOLID_NORMAL = 'M336 84L335 84.5L336 85L335 85.5L336 86L335 86.5L336 87L335 87.5L336 88L335 88.5L336 89L335 89.5L336 90L335 90.5L336 91L335 91.5L336 92L335 92.5L336 93L335 93.5L336 94L335 94.5L336 95L335 95.5L336 96L335 96.5L336 97L335 97.5L336 98L335 98.5L336 99L335 99.5L336 100L335 100.5L336 101L335 101.5L336 102L335 102.5L336 103L335 103.5L336 104L335 104.5L336 105L335 105.5L336 106L335 106.5L336 107L335 107.5L336 108L335 108.5L336 109L335 109.5L336 110L335 110.5L336 111L335 111.5L336 112L308 126L179.184 61.5918L196 154L195.833 154.083L196 155L195.833 155.083L196 156L195.833 156.083L196 157L195.833 157.083L196 158L195.833 158.083L196 159L195.833 159.083L196 160L195.833 160.083L196 161L195.833 161.083L196 162L195.833 162.083L196 163L195.833 163.083L196 164L195.833 164.083L196 165L195.833 165.083L196 166L195.833 166.083L196 167L195.833 167.083L196 168L195.833 168.083L196 169L195.833 169.083L196 170L195.833 170.083L196 171L195.833 171.083L196 172L195.833 172.083L196 173L195.833 173.083L196 174L195.833 174.083L196 175L195.833 175.083L196 176L195.833 176.083L196 177L195.833 177.083L196 178L195.833 178.083L196 179L195.833 179.083L196 180L195.833 180.083L196 181L195.833 181.083L196 182L168 196L0 112L1 111.5L0 111L1 110.5L0 110L1 109.5L0 109L1 108.5L0 108L1 107.5L0 107L1 106.5L0 106L1 105.5L0 105L1 104.5L0 104L1 103.5L0 103L1 102.5L0 102L1 101.5L0 101L1 100.5L0 100L1 99.5L0 99L1 98.5L0 98L1 97.5L0 97L1 96.5L0 96L1 95.5L0 95L1 94.5L0 94L1 93.5L0 93L1 92.5L0 92L1 91.5L0 91L1 90.5L0 90L1 89.5L0 89L1 88.5L0 88L1 87.5L0 87L1 86.5L0 86L1 85.5L0 85L1 84.5L0 84L28 70L156.798 134.387L140 42L140.166 41.916L140 41L140.166 40.916L140 40L140.166 39.916L140 39L140.166 38.916L140 38L140.166 37.916L140 37L140.166 36.916L140 36L140.166 35.916L140 35L140.166 34.916L140 34L140.166 33.916L140 33L140.166 32.916L140 32L140.166 31.916L140 31L140.166 30.916L140 30L140.166 29.916L140 29L140.166 28.916L140 28L140.166 27.916L140 27L140.166 26.916L140 26L140.166 25.916L140 25L140.166 24.916L140 24L140.166 23.916L140 23L140.166 22.916L140 22L140.166 21.916L140 21L140.166 20.916L140 20L140.166 19.916L140 19L140.166 18.916L140 18L140.166 17.916L140 17L140.166 16.916L140 16L140.166 15.916L140 15L140.166 14.916L140 14L168 0L336 84Z';
const N_SOLID_PRESSED = 'M336 84L335 84.5L336 85L335 85.5L336 86L335 86.5L336 87L335 87.5L336 88L335 88.5L336 89L335 89.5L336 90L335 90.5L336 91L335 91.5L336 92L335 92.5L336 93L335 93.5L336 94L335 94.5L336 95L335 95.5L336 96L335 96.5L336 97L335 97.5L336 98L308 112L176.39 46.1943L196 154L195.833 154.083L196 155L195.833 155.083L196 156L195.833 156.083L196 157L195.833 157.083L196 158L195.833 158.083L196 159L195.833 159.083L196 160L195.833 160.083L196 161L195.833 161.083L196 162L195.833 162.083L196 163L195.833 163.083L196 164L195.833 164.083L196 165L195.833 165.083L196 166L195.833 166.083L196 167L195.833 167.083L196 168L168 182L0 98L1 97.5L0 97L1 96.5L0 96L1 95.5L0 95L1 94.5L0 94L1 93.5L0 93L1 92.5L0 92L1 91.5L0 91L1 90.5L0 90L1 89.5L0 89L1 88.5L0 88L1 87.5L0 87L1 86.5L0 86L1 85.5L0 85L1 84.5L0 84L28 70L159.599 135.793L140 28L140.166 27.916L140 27L140.166 26.916L140 26L140.166 25.916L140 25L140.166 24.916L140 24L140.166 23.916L140 23L140.166 22.916L140 22L140.166 21.916L140 21L140.166 20.916L140 20L140.166 19.916L140 19L140.166 18.916L140 18L140.166 17.916L140 17L140.166 16.916L140 16L140.166 15.916L140 15L140.166 14.916L140 14L168 0L336 84Z';
const R_SOLID_NORMAL = 'M240.369 36.1846C262.726 47.3629 273.584 58.7711 273.221 68.9004C273.233 69.2338 273.232 69.5658 273.221 69.8965C273.233 70.2325 273.233 70.5671 273.221 70.9004C273.233 71.2338 273.232 71.5658 273.221 71.8965C273.233 72.2309 273.232 72.5638 273.221 72.8955C273.233 73.2318 273.233 73.5668 273.221 73.9004C273.233 74.2338 273.232 74.5658 273.221 74.8965C273.233 75.2325 273.233 75.5671 273.221 75.9004C273.233 76.2338 273.232 76.5658 273.221 76.8965C273.233 77.2325 273.233 77.5671 273.221 77.9004C273.233 78.2351 273.233 78.5684 273.221 78.9004C273.233 79.2338 273.232 79.5658 273.221 79.8965C273.233 80.2325 273.233 80.5671 273.221 80.9004C273.233 81.2338 273.232 81.5658 273.221 81.8965C273.233 82.2325 273.233 82.5671 273.221 82.9004C273.233 83.2351 273.233 83.5684 273.221 83.9004C273.233 84.2338 273.232 84.5658 273.221 84.8965C273.233 85.2325 273.233 85.5671 273.221 85.9004C273.233 86.2338 273.232 86.5658 273.221 86.8965C273.233 87.2325 273.233 87.5671 273.221 87.9004C273.233 88.2351 273.233 88.5684 273.221 88.9004C273.233 89.2338 273.232 89.5658 273.221 89.8965C273.233 90.2325 273.233 90.5671 273.221 90.9004C273.233 91.2338 273.232 91.5658 273.221 91.8965C273.233 92.2325 273.233 92.5671 273.221 92.9004C273.233 93.2351 273.233 93.5684 273.221 93.9004C273.233 94.2338 273.232 94.5658 273.221 94.8965C273.233 95.2325 273.233 95.5671 273.221 95.9004C273.485 103.141 268.102 109.764 257.169 115.23C231.323 128.153 193.846 126.215 165.415 112L138.277 181.139L102.093 163.046L102.396 162.197L102.093 162.046L102.396 161.197L102.093 161.046L102.396 160.197L102.093 160.046L102.396 159.197L102.093 159.046L102.396 158.197L102.093 158.046L102.396 157.197L102.093 157.046L102.396 156.197L102.093 156.046L102.396 155.197L102.093 155.046L102.396 154.197L102.093 154.046L102.396 153.197L102.093 153.046L102.396 152.197L102.093 152.046L102.396 151.197L102.093 151.046L102.396 150.197L102.093 150.046L102.396 149.197L102.093 149.046L102.396 148.197L102.093 148.046L102.396 147.197L102.093 147.046L102.396 146.197L102.093 146.046L102.396 145.197L102.093 145.046L102.396 144.197L102.093 144.046L102.396 143.197L102.093 143.046L102.396 142.197L102.093 142.046L102.396 141.197L102.093 141.046L102.396 140.197L102.093 140.046L102.396 139.197L102.093 139.046L102.396 138.197L102.093 138.046L102.396 137.197L102.093 137.046L102.396 136.197L102.093 136.046L102.396 135.197L102.093 135.046L115.557 97.4082L99.5078 89.3848L27.1387 125.569L0 112L1 111.5L0 111L1 110.5L0 110L1 109.5L0 109L1 108.5L0 108L1 107.5L0 107L1 106.5L0 106L1 105.5L0 105L1 104.5L0 104L1 103.5L0 103L1 102.5L0 102L1 101.5L0 101L1 100.5L0 100L1 99.5L0 99L1 98.5L0 98L1 97.5L0 97L1 96.5L0 96L1 95.5L0 95L1 94.5L0 94L1 93.5L0 93L1 92.5L0 92L1 91.5L0 91L1 90.5L0 90L1 89.5L0 89L1 88.5L0 88L1 87.5L0 87L1 86.5L0 86L1 85.5L0 85L1 84.5L0 84L168 0L240.369 36.1846ZM163.692 57.292L180.923 65.9072C193.196 72.0439 204.26 75.5585 213.438 76.5645L169.292 54.4922L163.692 57.292Z';
const R_SOLID_PRESSED = 'M240.369 36.1846C262.726 47.3629 273.584 58.7711 273.221 68.9004C273.233 69.2351 273.233 69.5684 273.221 69.9004C273.233 70.2338 273.232 70.5658 273.221 70.8965C273.233 71.2325 273.233 71.5671 273.221 71.9004C273.233 72.2334 273.232 72.5651 273.221 72.8955C273.233 73.2318 273.233 73.5668 273.221 73.9004C273.233 74.2351 273.233 74.5684 273.221 74.9004C273.233 75.2338 273.232 75.5658 273.221 75.8965C273.233 76.2325 273.233 76.5671 273.221 76.9004C273.233 77.2338 273.232 77.5658 273.221 77.8965C273.233 78.2325 273.233 78.5671 273.221 78.9004C273.233 79.2351 273.233 79.5684 273.221 79.9004C273.233 80.2338 273.232 80.5658 273.221 80.8965C273.233 81.2325 273.233 81.5671 273.221 81.9004C273.485 89.1414 268.102 95.7639 257.169 101.23C231.323 114.153 193.846 112.215 165.415 98L138.277 167.139L102.093 149.046L102.396 148.197L102.093 148.046L102.396 147.197L102.093 147.046L102.396 146.197L102.093 146.046L102.396 145.197L102.093 145.046L102.396 144.197L102.093 144.046L102.396 143.197L102.093 143.046L102.396 142.197L102.093 142.046L102.396 141.197L102.093 141.046L102.396 140.197L102.093 140.046L102.396 139.197L102.093 139.046L102.396 138.197L102.093 138.046L102.396 137.197L102.093 137.046L102.396 136.197L102.093 136.046L102.396 135.197L102.093 135.046L119.809 85.5342L99.5078 75.3848L27.1387 111.569L0 98L1 97.5L0 97L1 96.5L0 96L1 95.5L0 95L1 94.5L0 94L1 93.5L0 93L1 92.5L0 92L1 91.5L0 91L1 90.5L0 90L1 89.5L0 89L1 88.5L0 88L1 87.5L0 87L1 86.5L0 86L1 85.5L0 85L1 84.5L0 84L168 0L240.369 36.1846ZM149.692 50.292L180.923 65.9072C202.892 76.8918 220.985 79.4768 231.323 74.3076C231.465 74.2365 231.604 74.1634 231.74 74.0908C228.123 70.7388 222.404 67.0479 214.523 63.1074L169.292 40.4922L149.692 50.292Z';

// The exported swept solids are already projected into their local screen-space orientation.
const LEFT_N_SOLID_X = 103.648878;
const LEFT_N_SOLID_Y = 73.765242;
const RIGHT_N_SOLID_X = LEFT_N_SOLID_X + 426;
const RIGHT_N_SOLID_Y = LEFT_N_SOLID_Y + 200;
const R_SOLID_X = 331.433585;
const R_SOLID_Y = 180.927385;

// Keep the approved cross-hatch as one global field so the phase reads continuously N → R → N.
const HATCH_DIRECTION = { x: 0.5885843638383417, y: 0.808435802426522 };
const HATCH_NORMAL = { x: -0.808435802426522, y: 0.5885843638383417 };
const HATCH_SPACING = 5.357116234906698;
const HATCH_LENGTH = 1500;
const hatchCorners = [
    { x: VIEWBOX_X, y: VIEWBOX_Y },
    { x: VIEWBOX_X + VIEWBOX_WIDTH, y: VIEWBOX_Y },
    { x: VIEWBOX_X, y: VIEWBOX_Y + VIEWBOX_HEIGHT },
    { x: VIEWBOX_X + VIEWBOX_WIDTH, y: VIEWBOX_Y + VIEWBOX_HEIGHT },
];
const hatchProjection = hatchCorners.map(({ x, y }) => x * HATCH_NORMAL.x + y * HATCH_NORMAL.y);
const hatchMin = Math.min(...hatchProjection) - 120;
const hatchMax = Math.max(...hatchProjection) + 120;
const hatchStart = Math.floor(hatchMin / HATCH_SPACING) * HATCH_SPACING;
const hatchSegments: string[] = [];
for (let offset = hatchStart; offset <= hatchMax; offset += HATCH_SPACING) {
    const cx = HATCH_NORMAL.x * offset;
    const cy = HATCH_NORMAL.y * offset;
    const x1 = cx - HATCH_DIRECTION.x * HATCH_LENGTH;
    const y1 = cy - HATCH_DIRECTION.y * HATCH_LENGTH;
    const x2 = cx + HATCH_DIRECTION.x * HATCH_LENGTH;
    const y2 = cy + HATCH_DIRECTION.y * HATCH_LENGTH;
    hatchSegments.push(`M ${x1.toFixed(3)} ${y1.toFixed(3)} L ${x2.toFixed(3)} ${y2.toFixed(3)}`);
}
const GLOBAL_HATCH_PATH = hatchSegments.join(' ');
---

<div class="nrn-stage" data-nrn-stage>
    <button
        class="nrn-mark"
        type="button"
        data-nrn-mark
        aria-label="Press the interactive NRN mark"
        aria-pressed="false"
    >
        <svg
            class="nrn-svg"
            data-nrn-svg
            viewBox={`${VIEWBOX_X} ${VIEWBOX_Y} ${VIEWBOX_WIDTH} ${VIEWBOX_HEIGHT}`}
            preserveAspectRatio="xMidYMid meet"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
            role="img"
            aria-label="Continuous isometric NRN monogram"
        >
            <defs>
                <clipPath id="nrn-surface-clip" clipPathUnits="userSpaceOnUse">
                    <path d={N_FRONT_PATH} transform={LEFT_N_MATRIX} />
                    <path d={R_FRONT_PATH} transform={R_MATRIX} fill-rule="evenodd" clip-rule="evenodd" />
                    <path d={N_FRONT_PATH} transform={RIGHT_N_MATRIX} />
                </clipPath>
                <radialGradient
                    id="nrn-radial-gradient"
                    data-nrn-gradient
                    cx="472"
                    cy="280"
                    r="300"
                    gradientUnits="userSpaceOnUse"
                >
                    <stop offset="0" class="nrn-glow-start" />
                    <stop offset="1" class="nrn-glow-end" />
                </radialGradient>
            </defs>

            <g class="nrn-guides" aria-hidden="true">
                <path d="M 24 568 L 936 42" />
                <path d="M -20 132 L 894 660" />
                <path d="M 174 -24 L 948 422" />
            </g>

            <!-- Keep guides from showing through the button body. The normal swept volume
                 is a superset of the pressed volume, so it is a stable occlusion mask. -->
            <g class="nrn-solid-occlusion" aria-hidden="true">
                <path d={N_SOLID_NORMAL} transform={`translate(${LEFT_N_SOLID_X} ${LEFT_N_SOLID_Y})`} />
                <path d={R_SOLID_NORMAL} transform={`translate(${R_SOLID_X} ${R_SOLID_Y})`} fill-rule="evenodd" />
                <path d={N_SOLID_NORMAL} transform={`translate(${RIGHT_N_SOLID_X} ${RIGHT_N_SOLID_Y})`} />
            </g>

            <!-- Authored swept-solid normal state from Figma. -->
            <g data-solid-normal aria-hidden="true">
                <g class="nrn-solid-base">
                    <path d={N_SOLID_NORMAL} transform={`translate(${LEFT_N_SOLID_X} ${LEFT_N_SOLID_Y})`} />
                    <path d={R_SOLID_NORMAL} transform={`translate(${R_SOLID_X} ${R_SOLID_Y})`} fill-rule="evenodd" />
                    <path d={N_SOLID_NORMAL} transform={`translate(${RIGHT_N_SOLID_X} ${RIGHT_N_SOLID_Y})`} />
                </g>
                <g class="nrn-solid-glow">
                    <path d={N_SOLID_NORMAL} transform={`translate(${LEFT_N_SOLID_X} ${LEFT_N_SOLID_Y})`} />
                    <path d={R_SOLID_NORMAL} transform={`translate(${R_SOLID_X} ${R_SOLID_Y})`} fill-rule="evenodd" />
                    <path d={N_SOLID_NORMAL} transform={`translate(${RIGHT_N_SOLID_X} ${RIGHT_N_SOLID_Y})`} />
                </g>
            </g>

            <!-- Authored swept-solid pressed state from Figma. Bottom remains fixed;
                 only 14px of physical depth remains. -->
            <g data-solid-pressed aria-hidden="true" opacity="0">
                <g class="nrn-solid-base">
                    <path d={N_SOLID_PRESSED} transform={`translate(${LEFT_N_SOLID_X} ${LEFT_N_SOLID_Y + PRESS_DISTANCE})`} />
                    <path d={R_SOLID_PRESSED} transform={`translate(${R_SOLID_X} ${R_SOLID_Y + PRESS_DISTANCE})`} fill-rule="evenodd" />
                    <path d={N_SOLID_PRESSED} transform={`translate(${RIGHT_N_SOLID_X} ${RIGHT_N_SOLID_Y + PRESS_DISTANCE})`} />
                </g>
                <g class="nrn-solid-glow">
                    <path d={N_SOLID_PRESSED} transform={`translate(${LEFT_N_SOLID_X} ${LEFT_N_SOLID_Y + PRESS_DISTANCE})`} />
                    <path d={R_SOLID_PRESSED} transform={`translate(${R_SOLID_X} ${R_SOLID_Y + PRESS_DISTANCE})`} fill-rule="evenodd" />
                    <path d={N_SOLID_PRESSED} transform={`translate(${RIGHT_N_SOLID_X} ${RIGHT_N_SOLID_Y + PRESS_DISTANCE})`} />
                </g>
            </g>

            <!-- Exact approved top surface. This is the only rigid part that translates
                 continuously during the spring press. -->
            <g data-nrn-surface>
                <g class="nrn-front-fill" aria-hidden="true">
                    <path d={N_FRONT_PATH} transform={LEFT_N_MATRIX} />
                    <path d={R_FRONT_PATH} transform={R_MATRIX} fill-rule="evenodd" />
                    <path d={N_FRONT_PATH} transform={RIGHT_N_MATRIX} />
                </g>

                <path
                    class="nrn-global-hatch"
                    d={GLOBAL_HATCH_PATH}
                    clip-path="url(#nrn-surface-clip)"
                    aria-hidden="true"
                />

                <g class="nrn-top-base" aria-hidden="true">
                    <path d={N_FRONT_PATH} transform={LEFT_N_MATRIX} />
                    <path d={R_FRONT_PATH} transform={R_MATRIX} fill-rule="evenodd" />
                    <path d={N_FRONT_PATH} transform={RIGHT_N_MATRIX} />
                </g>
                <g class="nrn-top-glow" aria-hidden="true">
                    <path d={N_FRONT_PATH} transform={LEFT_N_MATRIX} />
                    <path d={R_FRONT_PATH} transform={R_MATRIX} fill-rule="evenodd" />
                    <path d={N_FRONT_PATH} transform={RIGHT_N_MATRIX} />
                </g>
            </g>

            <text class="nrn-fig-label" x="78" y="495">FIG. 01 / NRN SYSTEM</text>
        </svg>
    </button>

    <div class="nrn-note" aria-hidden="true">
        <svg viewBox="0 0 54 54">
            <path d="M49 6C37 8 28 14 23 23C18 31 19 39 24 45" />
            <path d="M17 39L24 46L31 39" />
        </svg>
        <span>
            follows your cursor
            <small>click the mark</small>
        </span>
    </div>
</div>

<style>
    .nrn-stage {
        position: relative;
        width: 100%;
        height: 100%;
        overflow: hidden;
        color: var(--text);
        background: var(--bg);
        --nrn-top-stroke: color-mix(in srgb, var(--text) 34%, var(--bg));
        --nrn-solid-stroke: color-mix(in srgb, var(--text) 28%, var(--bg));
        --nrn-hatch: color-mix(in srgb, var(--text) 24%, var(--bg));
    }

    .nrn-mark {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        border: 0;
        padding: 0;
        background: transparent;
        color: var(--text);
        cursor: pointer;
        touch-action: manipulation;
        -webkit-tap-highlight-color: transparent;
    }

    .nrn-mark:focus-visible {
        outline: 1px solid color-mix(in srgb, var(--text) 62%, transparent);
        outline-offset: -5px;
    }

    .nrn-svg {
        display: block;
        width: 100%;
        height: 100%;
        overflow: visible;
    }

    .nrn-guides {
        fill: none;
        stroke: color-mix(in srgb, var(--text) 7%, var(--bg));
        stroke-width: 1;
        stroke-dasharray: 5 5;
        vector-effect: non-scaling-stroke;
        pointer-events: none;
    }

    .nrn-solid-occlusion,
    .nrn-front-fill {
        fill: var(--bg);
        stroke: none;
    }

    .nrn-solid-base,
    .nrn-solid-glow,
    .nrn-top-base,
    .nrn-top-glow,
    .nrn-global-hatch {
        fill: none;
        stroke-linecap: round;
        stroke-linejoin: round;
        vector-effect: non-scaling-stroke;
        pointer-events: none;
    }

    .nrn-solid-base {
        stroke: var(--nrn-solid-stroke);
        stroke-width: 1.3;
    }

    .nrn-solid-glow {
        stroke: url(#nrn-radial-gradient);
        stroke-width: 1.3;
    }

    .nrn-top-base {
        stroke: var(--nrn-top-stroke);
        stroke-width: 1.45;
    }

    .nrn-top-glow {
        stroke: url(#nrn-radial-gradient);
        stroke-width: 1.45;
    }

    .nrn-global-hatch {
        stroke: var(--nrn-hatch);
        stroke-width: 0.5;
        opacity: 0.74;
    }

    .nrn-glow-start {
        stop-color: var(--text);
        stop-opacity: 1;
    }

    .nrn-glow-end {
        stop-color: color-mix(in srgb, var(--text) 40%, var(--bg));
        stop-opacity: 0;
    }

    .nrn-fig-label {
        fill: color-mix(in srgb, var(--muted) 50%, var(--bg));
        font-family: var(--font-mono);
        font-size: 11px;
        font-weight: 500;
        letter-spacing: 0.04em;
        user-select: none;
    }

    .nrn-note {
        position: absolute;
        right: 3%;
        top: 58%;
        display: flex;
        align-items: flex-end;
        gap: 5px;
        color: color-mix(in srgb, var(--text) 72%, var(--muted));
        font-family: "Segoe Print", "Bradley Hand", "Comic Sans MS", cursive;
        font-size: clamp(12px, 1vw, 16px);
        font-style: italic;
        line-height: 1.2;
        transform: rotate(-3deg);
        pointer-events: none;
        user-select: none;
    }

    .nrn-note span {
        display: flex;
        flex-direction: column;
    }

    .nrn-note small {
        margin-top: 2px;
        font: inherit;
        opacity: 0.76;
    }

    .nrn-note svg {
        width: 50px;
        height: 50px;
        overflow: visible;
        fill: none;
        stroke: currentColor;
        stroke-width: 1.05;
        stroke-linecap: round;
        stroke-linejoin: round;
        opacity: 0.78;
        transform: rotate(-12deg) translateY(4px);
    }

    @media (max-width: 720px) {
        .nrn-note {
            top: auto;
            right: 3%;
            bottom: 8%;
        }
    }

    @media (hover: none) {
        .nrn-note {
            display: none;
        }
    }
</style>

<script>
    type SpringState = { value: number; velocity: number };

    const VIEW_X = 60;
    const VIEW_Y = 45;
    const VIEW_W = 825;
    const VIEW_H = 470;
    const PRESS_DEPTH = 14;

    const springAdvance = (
        state: SpringState,
        target: number,
        stiffness: number,
        damping: number,
        mass: number,
        dt: number,
    ) => {
        const substep = 1 / 240;
        const steps = Math.max(1, Math.ceil(dt / substep));
        const h = dt / steps;
        for (let index = 0; index < steps; index += 1) {
            const force = -stiffness * (state.value - target) - damping * state.velocity;
            const acceleration = force / mass;
            state.velocity += acceleration * h;
            state.value += state.velocity * h;
        }
    };

    const clamp01 = (value: number) => Math.min(1, Math.max(0, value));
    const smoothstep = (edge0: number, edge1: number, value: number) => {
        const x = clamp01((value - edge0) / Math.max(edge1 - edge0, 0.0001));
        return x * x * (3 - 2 * x);
    };

    const setupNRN = (root: HTMLElement) => {
        if (root.dataset.nrnReady === 'true') return;
        root.dataset.nrnReady = 'true';

        const mark = root.querySelector<HTMLButtonElement>('[data-nrn-mark]');
        const svg = root.querySelector<SVGSVGElement>('[data-nrn-svg]');
        const surface = root.querySelector<SVGGElement>('[data-nrn-surface]');
        const normalSolid = root.querySelector<SVGGElement>('[data-solid-normal]');
        const pressedSolid = root.querySelector<SVGGElement>('[data-solid-pressed]');
        const gradient = root.querySelector<SVGRadialGradientElement>('[data-nrn-gradient]');
        if (!mark || !svg || !surface || !normalSolid || !pressedSolid || !gradient) return;

        const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
        const finePointer = window.matchMedia('(hover: hover) and (pointer: fine)');
        const cursorX: SpringState = { value: VIEW_X + VIEW_W / 2, velocity: 0 };
        const cursorY: SpringState = { value: VIEW_Y + VIEW_H / 2, velocity: 0 };
        const press: SpringState = { value: 0, velocity: 0 };

        let cursorTargetX = cursorX.value;
        let cursorTargetY = cursorY.value;
        let pressTarget = 0;
        let pressed = false;
        let inView = true;
        let raf = 0;
        let lastTime = performance.now();
        let audioContext: AudioContext | null = null;

        const onPointerMove = (event: PointerEvent) => {
            if (!finePointer.matches) return;
            const rect = svg.getBoundingClientRect();
            if (rect.width <= 0 || rect.height <= 0) return;
            cursorTargetX = VIEW_X + ((event.clientX - rect.left) / rect.width) * VIEW_W;
            cursorTargetY = VIEW_Y + ((event.clientY - rect.top) / rect.height) * VIEW_H;
        };

        const playClick = () => {
            try {
                audioContext ??= new AudioContext();
                const now = audioContext.currentTime;
                const oscillator = audioContext.createOscillator();
                const gain = audioContext.createGain();
                oscillator.type = 'triangle';
                oscillator.frequency.setValueAtTime(210, now);
                oscillator.frequency.exponentialRampToValueAtTime(82, now + 0.045);
                gain.gain.setValueAtTime(0.0001, now);
                gain.gain.exponentialRampToValueAtTime(0.05, now + 0.004);
                gain.gain.exponentialRampToValueAtTime(0.0001, now + 0.075);
                oscillator.connect(gain);
                gain.connect(audioContext.destination);
                oscillator.start(now);
                oscillator.stop(now + 0.08);
            } catch {
                // Decorative only.
            }
        };

        const pressDown = () => {
            if (pressed) return;
            pressed = true;
            pressTarget = PRESS_DEPTH;
            mark.setAttribute('aria-pressed', 'true');
        };

        const release = (withSound = true) => {
            if (!pressed) return;
            pressed = false;
            pressTarget = 0;
            mark.setAttribute('aria-pressed', 'false');
            if (withSound) playClick();
        };

        window.addEventListener('pointermove', onPointerMove, { passive: true });
        mark.addEventListener('pointerdown', (event) => {
            if (event.button !== 0) return;
            mark.setPointerCapture?.(event.pointerId);
            pressDown();
        });
        mark.addEventListener('pointerup', (event) => {
            if (mark.hasPointerCapture?.(event.pointerId)) mark.releasePointerCapture?.(event.pointerId);
            release(true);
        });
        mark.addEventListener('pointercancel', () => release(false));
        mark.addEventListener('lostpointercapture', () => release(false));
        mark.addEventListener('keydown', (event) => {
            if (event.key !== 'Enter' && event.key !== ' ') return;
            event.preventDefault();
            pressDown();
        });
        mark.addEventListener('keyup', (event) => {
            if (event.key !== 'Enter' && event.key !== ' ') return;
            event.preventDefault();
            release(true);
        });
        mark.addEventListener('blur', () => release(false));

        const observer = new IntersectionObserver(
            ([entry]) => {
                inView = entry?.isIntersecting ?? true;
            },
            { rootMargin: '80px', threshold: 0.01 },
        );
        observer.observe(root);

        const renderPress = (value: number) => {
            surface.setAttribute('transform', `translate(0 ${value.toFixed(3)})`);

            const progress = clamp01(value / PRESS_DEPTH);
            // Narrow crossfade between the two authored solid states. Endpoints remain
            // pixel-identical to Figma; the top surface provides the continuous motion.
            const pressedMix = smoothstep(0.28, 0.72, progress);
            normalSolid.setAttribute('opacity', (1 - pressedMix).toFixed(4));
            pressedSolid.setAttribute('opacity', pressedMix.toFixed(4));
        };

        const tick = (now: number) => {
            const dt = Math.min((now - lastTime) / 1000, 0.05);
            lastTime = now;

            if (reducedMotion.matches) {
                cursorX.value = cursorTargetX;
                cursorY.value = cursorTargetY;
                cursorX.velocity = 0;
                cursorY.velocity = 0;
                press.value = pressTarget;
                press.velocity = 0;
            } else {
                springAdvance(cursorX, cursorTargetX, 300, 30, 0.1, dt);
                springAdvance(cursorY, cursorTargetY, 300, 30, 0.1, dt);
                springAdvance(press, pressTarget, 200, 18, 0.5, dt);
            }

            if (inView) {
                gradient.setAttribute('cx', cursorX.value.toFixed(2));
                gradient.setAttribute('cy', cursorY.value.toFixed(2));
                renderPress(press.value);
            }
            raf = requestAnimationFrame(tick);
        };

        renderPress(0);
        raf = requestAnimationFrame(tick);

        window.addEventListener(
            'pagehide',
            () => {
                cancelAnimationFrame(raf);
                observer.disconnect();
                window.removeEventListener('pointermove', onPointerMove);
            },
            { once: true },
        );
    };

    document.querySelectorAll<HTMLElement>('[data-nrn-stage]').forEach(setupNRN);
</script>
