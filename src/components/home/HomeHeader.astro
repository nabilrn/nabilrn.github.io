---
import SiteSearch from '../SiteSearch.astro';
import ThemeToggle from '../ThemeToggle.astro';
import {
    getSiteContent,
    localeMeta,
    localizePath,
    normalizeLocale,
    supportedLocales,
    type SiteLocale,
} from '../../data/siteContent';

interface Props {
    locale?: SiteLocale;
}

const locale = normalizeLocale(Astro.props.locale);
const content = getSiteContent(locale);
const homePath = localizePath('/', locale);
const projectsPath = localizePath('/projects/', locale);
const blogPath = localizePath('/blog/', locale);
---

<header class="home-header">
    <div class="home-header-inner">
        <a href={homePath} class="home-brand" aria-label={content.nav.home}>
            <span class="home-brand-mark" aria-hidden="true">NR</span>
            <span>nabilrn</span>
        </a>

        <nav class="home-nav" aria-label={content.nav.ariaPageNavigation}>
            <a href={homePath} class="home-nav-link is-active">{content.nav.overview}</a>
            <a href={projectsPath} class="home-nav-link">{content.nav.projects}</a>
            <a href={blogPath} class="home-nav-link">{content.nav.blog}</a>
        </nav>

        <div class="home-actions">
            <ThemeToggle locale={locale} />
            <SiteSearch locale={locale} />

            <details class="home-language">
                <summary aria-label={content.nav.ariaLanguageNavigation}>{locale.toUpperCase()}</summary>
                <div class="home-language-menu">
                    {supportedLocales.map((item) => (
                        <a
                            class:list={['home-language-option', { 'is-active': item === locale }]}
                            href={localizePath('/', item)}
                        >
                            <span>{item.toUpperCase()}</span>
                            <span>{localeMeta[item].label}</span>
                        </a>
                    ))}
                </div>
            </details>
        </div>
    </div>
</header>

<style>
    .home-header {
        position: relative;
        z-index: 30;
        width: 100%;
        height: 52px;
        background: var(--bg);
        border: 2px solid var(--border);
    }

    .home-header-inner {
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0 clamp(14px, 1.5vw, 24px);
        display: grid;
        grid-template-columns: minmax(160px, 1fr) auto minmax(160px, 1fr);
        align-items: center;
    }

    .home-brand {
        display: inline-flex;
        width: max-content;
        align-items: center;
        gap: 9px;
        color: var(--text);
        font-family: var(--font-sans);
        font-size: 15px;
        font-weight: 600;
        letter-spacing: -0.03em;
    }

    .home-brand-mark {
        width: 24px;
        height: 24px;
        display: grid;
        place-items: center;
        border: 1px solid var(--border-strong);
        color: var(--muted);
        font-family: var(--font-mono);
        font-size: 8px;
        font-weight: 500;
        line-height: 1;
        letter-spacing: -0.03em;
    }

    .home-nav {
        display: flex;
        align-items: stretch;
        align-self: stretch;
        gap: 28px;
    }

    .home-nav-link {
        position: relative;
        display: inline-flex;
        align-items: center;
        color: var(--muted);
        font-family: var(--font-sans);
        font-size: 11px;
        line-height: 1;
        transition: color 140ms ease;
    }

    .home-nav-link:hover,
    .home-nav-link.is-active {
        color: var(--text);
    }

    .home-nav-link.is-active::after {
        content: '';
        position: absolute;
        left: 0;
        right: 0;
        bottom: -2px;
        height: 2px;
        background: var(--text);
    }

    .home-actions {
        display: flex;
        justify-content: flex-end;
        align-items: center;
        gap: 8px;
    }

    .home-language {
        position: relative;
    }

    .home-language summary {
        width: 30px;
        height: 26px;
        display: grid;
        place-items: center;
        list-style: none;
        border: 1px solid var(--border-strong);
        border-radius: 0;
        color: var(--muted);
        background: transparent;
        cursor: pointer;
        font-family: var(--font-mono);
        font-size: 9px;
        line-height: 1;
        transition: border-color 140ms ease, color 140ms ease, background 140ms ease;
    }

    .home-language summary::-webkit-details-marker {
        display: none;
    }

    .home-language summary:hover,
    .home-language[open] summary {
        color: var(--text);
        border-color: var(--muted);
        background: var(--surface);
    }

    .home-language-menu {
        position: absolute;
        top: calc(100% + 7px);
        right: 0;
        width: 170px;
        padding: 4px;
        display: grid;
        gap: 1px;
        background: var(--surface);
        border: 1px solid var(--border-strong);
        border-radius: 0;
        box-shadow: var(--shadow);
    }

    .home-language-option {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        padding: 7px 8px;
        border-radius: 0;
        color: var(--muted);
        font-family: var(--font-sans);
        font-size: 10px;
    }

    .home-language-option:hover,
    .home-language-option.is-active {
        color: var(--text);
        background: var(--surface-2);
    }

    :global(.home-header .theme-toggle),
    :global(.home-header .site-search-trigger) {
        width: 26px;
        min-width: 26px;
        height: 26px;
        min-height: 26px;
        border-radius: 0;
        background: transparent;
        border-color: var(--border-strong);
    }

    :global(.home-header .theme-toggle svg) {
        width: 12px;
        height: 12px;
        stroke-width: 1.6;
    }

    :global(.home-header .site-search-trigger) {
        padding: 0;
        justify-content: center;
        font-family: var(--font-sans);
    }

    :global(.home-header .site-search-trigger > span),
    :global(.home-header .site-search-trigger > kbd) {
        display: none;
    }

    :global(.home-header .site-search-trigger svg) {
        width: 12px;
        height: 12px;
    }

    @media (max-width: 760px) {
        .home-header {
            height: 48px;
            border-right: 0;
            border-left: 0;
        }

        .home-header-inner {
            grid-template-columns: 1fr auto;
            padding: 0 12px;
        }

        .home-nav {
            display: none;
        }

        .home-brand {
            font-size: 14px;
        }
    }
</style>
