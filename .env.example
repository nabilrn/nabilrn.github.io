# Optional override for the shared metrics API used by homepage analytics and blog engagement.
# Production defaults to the original public metrics Worker when unset.
# Local development leaves metrics unavailable unless this value is configured.
# PUBLIC_ENGAGEMENT_API_BASE=https://portfolio-metrics-api.nabilrizkinavisa.workers.dev
