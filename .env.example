# Optional: shared Metrics Worker API base for homepage analytics and blog engagement.
# Leave unset during local development to keep metrics unavailable instead of writing to production.
# Example:
# PUBLIC_ENGAGEMENT_API_BASE=https://portfolio-metrics-api.<your-workers-subdomain>.workers.dev
PUBLIC_ENGAGEMENT_API_BASE=
